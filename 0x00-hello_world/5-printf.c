#include <stdio.h>

/**
 * main - Entry point for the program
 *
 * This program prints the specified text with proper grammar
 * and the outcome is a piece of art, followed by a new line.
 *
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
